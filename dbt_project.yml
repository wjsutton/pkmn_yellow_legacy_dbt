name: 'pkmn_yellow_legacy_map'
version: '1.0.0'
profile: 'pkmn_yellow_legacy_map'

clean-targets:
  - "target"
  - "dbt_packages"

seeds:
  +delimiter: ','
  +schema: raw
  +post-hook: "{{ add_dbt_loaded_at_col() }}"

models:
  pkmn_yellow_legacy_map:
    staging:
      +materialized: table
      +schema: staging
    intermediate:
      +materialized: table
      +schema: intermediate
    optimisation:
      +materialized: table
      +schema: optimisation