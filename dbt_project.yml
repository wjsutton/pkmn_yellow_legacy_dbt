name: 'pkmn_yellow_legacy_map'
version: '1.0.0'
profile: 'pkmn_yellow_legacy_map'

clean-targets:
  - "target"
  - "dbt_packages"

seeds:
  +delimiter: ','
  +schema: raw

models:
  pkmn_yellow_legacy_map:
    staging:
      +materialized: table
      +schema: staging
    intermediate:
      +materialized: table
      +schema: intermediate
    marts:
      +materialized: table
      +schema: marts
    dashboard:
      +materialized: table
      +schema: dashboard
      +enabled: false
